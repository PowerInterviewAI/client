# Local Environment Variables (overrides .env.development)
# This file is gitignored and is for your personal local configuration

# API Base URL for backend server (used by Electron main process)
API_BASE_URL=http://localhost:8080/api
